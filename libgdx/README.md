# BattleshipGDX

Minimal libGDX demo screens (Menu/Difficulty/Setup/Battle) using Scene2D.

## Run (desktop)

From the libgdx folder:

```bash
gradle :desktop:run
```

If gradle is not installed, install Gradle or generate a wrapper:

```bash
gradle wrapper
./gradlew :desktop:run
```
